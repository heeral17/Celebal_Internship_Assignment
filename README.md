"Loaded the CSV file -Dataset had 1996 rows across 24 columns.
Explored the dataset -By using functions (info,shape, head, tail,etc)
Handled missing data-Filled missing values in price and category columns
Handled Duplication -Removed  996 duplicate rows leaving 1000 clean records. 
Added an additional information in data -Created total_amount as price × quantity."
Saved the cleaned data
